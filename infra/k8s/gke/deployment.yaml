---
apiVersion: v1
kind: Secret
metadata:
  name: confident-meninsky-418636641f-bst-token
  namespace: apps-release
  labels:
    app: confident-meninsky-418636641f
    managed-by: backstract
  annotations:
    backstract.io/token-scope: "storage.*,cache.*"
type: Opaque
stringData:
  token: "PLACEHOLDER_BST_TOKEN_REPLACE_AT_RUNTIME"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: confident-meninsky-418636641f-depl
  namespace: apps-release
  labels:
    app: confident-meninsky-418636641f
    release: prometheus
spec:
  replicas: 1
  selector:
    matchLabels:
      app: confident-meninsky-418636641f
      release: prometheus
  template:
    metadata:
      labels:
        app: confident-meninsky-418636641f
        release: prometheus
    spec:
      #runtimeClassName: gvisor
      #imagePullSecrets:
        #- name: aws-ecr-creds
      serviceAccountName: alloy-service-account
      volumes:
        - name: alloy-config
          configMap:
            name: alloy-configmap-confident-meninsky-418636641f
            items:
              - key: alloy.river
                path: config.alloy
        - name: podinfo
          downwardAPI:
            items:
              - path: "labels"
                fieldRef:
                  fieldPath: metadata.labels
              - path: "annotations"
                fieldRef:
                  fieldPath: metadata.annotations
      containers:
        - name: confident-meninsky-418636641f
          image: asia-south2-docker.pkg.dev/platform-test-487912/backstract-apps/backstract_apps:confident-meninsky-418636641f
          imagePullPolicy: Always
          env:
            - name: BACKSTRACT_AUTH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: confident-meninsky-418636641f-bst-token
                  key: token
            - name: BACKSTRACT_PLATFORM_URL
              value: "https://api-release.beemerbenzbentley.site/sigma"
          envFrom:
            - configMapRef:
                name: env-configmap-coll-e2b69ba092b24cabb5a42d3cbf073676-deployment-84cf2118-0a45-4d75-a1fb-a710066e4a9e
            - secretRef:
                name: app-secrets-coll-e2b69ba092b24cabb5a42d3cbf073676
            - secretRef:
                name: app-resources-secrets-coll-e2b69ba092b24cabb5a42d3cbf073676
                optional: true
          ports:
            - containerPort: 8080
              name: http
            - containerPort: 8080
              name: metrics
          resources:
            requests:
              memory: "250Mi"
              cpu: "50m"
            limits:
              memory: "250Mi"
              cpu: "1"
        - name: alloy-sidecar
          image: grafana/alloy:latest
          args:
            - "run"
            - "/etc/alloy/config.alloy"
            - "--server.http.listen-addr=0.0.0.0:12345"
          ports:
            - name: otlp-grpc
              containerPort: 4317
              protocol: TCP
            - name: otlp-http
              containerPort: 4318
              protocol: TCP
            - name: alloy-http
              containerPort: 12345
              protocol: TCP
          volumeMounts:
            - name: alloy-config
              mountPath: /etc/alloy
            - name: podinfo
              mountPath: /etc/podinfo
              readOnly: true
          resources:
            requests:
              memory: "50Mi"
              cpu: "25m"
            limits:
              memory: "128Mi"
              cpu: "100m"
